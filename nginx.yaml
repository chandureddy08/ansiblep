- name: install and start nginx
  hosts: web
  become: yes #root
  tasks:
  - name: install nginx
    ansible.builtin.dnf:
      name: nginx
      state: latest
  - name: nginx
    ansible.builtin.service:
      name: nginx
      state: stopped
      enabled: no