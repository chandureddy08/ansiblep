- name: install and start nginx
  hosts: web
  become: yes #root
  tasks:
  - name: install nginx
    ansible.builtin.dnf:
      name: nginx
      status: installed
  - name: start nginx
    ansible.builtin.service:
      name: start nginx
      status: started